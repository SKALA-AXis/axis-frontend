# 로컬 개발: backend dev server 직접 가리킴.
# 운영 (cluster): empty 으로 두면 frontend 가 relative URL (`/api/...`) 사용 →
#   nginx (frontend pod) → backend service 로 proxy. cluster 배포 시 .env 없으면
#   httpClient 가 자동 relative URL fallback (src/shared/api/httpClient.ts).
VITE_API_BASE_URL=http://localhost:8080
