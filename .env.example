# Set this when the browser should call the backend directly.
# Leave it empty to use the Vite dev proxy for `/api`.
VITE_API_BASE_URL=

# Optional Vite dev proxy target. Useful when backend runs on a non-default port.
VITE_API_PROXY_TARGET=http://127.0.0.1:18080
